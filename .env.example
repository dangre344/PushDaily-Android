# Copy to .env and fill in.
# Only PUBLIC values belong here — they ship inside the app bundle.
# Database and LLM keys live as Cloudflare Worker secrets, never in the app.

EXPO_PUBLIC_TRAINER_WORKER_URL=
EXPO_PUBLIC_TRAINER_APP_TOKEN=
